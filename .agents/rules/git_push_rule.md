# Git Push Rule

- **DO NOT PUSH TO GITHUB (`git push`) UNLESS THE USER EXPLICITLY REQUESTS IT.**
- Perform local commits (`git commit`) when work is done, but NEVER run `git push` without explicit user instruction.
